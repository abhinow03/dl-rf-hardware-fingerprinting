# ── code + docs only; artifacts, checkpoints, data caches excluded ──
__pycache__/
*.py[cod]
*.pt
*.pth
*.npz
*.npy
*.pkl
*.log
*.jsonl
.ipynb_checkpoints/

# python packaging / editable-install artifacts
build/
dist/
*.egg-info/
.eggs/

# pipeline outputs & frozen checkpoint stores (off-repo, machine-local; see rffp.config)
/results_gen/
/runs_gen/
runs/

# legacy pre-release trees left on disk after the src/ reorg (untracked; hold gitignored assets)
/summer_work/
/gen_rff/
/demo/
/results/
